---
phase: 34-per-box-bool-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/btop_shared.hpp
  - src/linux/btop_collect.cpp
  - src/osx/btop_collect.cpp
  - src/freebsd/btop_collect.cpp
  - src/netbsd/btop_collect.cpp
  - src/openbsd/btop_collect.cpp
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05, MIGR-06]

must_haves:
  truths:
    - "No extern redraw declarations exist in btop_shared.hpp for any box namespace"
    - "All 35 collector redraw=true sites replaced with Runner::mark_dirty() calls"
    - "Namespace-scope redraw variable definitions in btop_draw.cpp are preserved (file-local)"
    - "Draw function self-invalidation (Net IP change, Proc selection) still works"
    - "Full test suite passes with zero regressions"
  artifacts:
    - path: "src/btop_shared.hpp"
      provides: "Cleaned extern declarations (5 removed)"
      contains: "mark_dirty"
    - path: "src/linux/btop_collect.cpp"
      provides: "Migrated collector sites"
      contains: "Runner::mark_dirty"
    - path: "src/osx/btop_collect.cpp"
      provides: "Migrated collector sites"
      contains: "Runner::mark_dirty"
    - path: "src/freebsd/btop_collect.cpp"
      provides: "Migrated collector sites"
      contains: "Runner::mark_dirty"
    - path: "src/netbsd/btop_collect.cpp"
      provides: "Migrated collector sites"
      contains: "Runner::mark_dirty"
    - path: "src/openbsd/btop_collect.cpp"
      provides: "Migrated collector sites"
      contains: "Runner::mark_dirty"
  key_links:
    - from: "src/*/btop_collect.cpp"
      to: "Runner::mark_dirty()"
      via: "direct call replacing bool assignment"
      pattern: "Runner::mark_dirty\\(DirtyBit::(Mem|Net|Proc)\\)"
    - from: "src/btop_shared.hpp"
      to: "src/btop_draw.cpp"
      via: "extern declarations removed, definitions remain file-local"
      pattern: "extern.*redraw"
---

<objective>
Remove all five per-box namespace redraw bool extern declarations and migrate all 35 collector write sites to Runner::mark_dirty().

Purpose: Complete the unified redraw migration by eliminating the last cross-thread bool communication between collectors and draw functions. After this, PendingDirty is the sole mechanism for signaling redraw needs.

Output: All collector sites use Runner::mark_dirty(), all extern declarations removed, namespace-scope definitions preserved as file-local in btop_draw.cpp.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-runner-integration/32-01-SUMMARY.md
@.planning/phases/33-calcsizes-decoupling/33-01-SUMMARY.md
@.planning/phases/34-per-box-bool-migration/34-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/btop_shared.hpp (Runner API, line 460):
```cpp
void mark_dirty(DirtyBit bits) noexcept;
```

From src/btop_dirty.hpp (DirtyBit enum):
```cpp
enum class DirtyBit : uint32_t {
    Cpu           = 1u << 0,
    Mem           = 1u << 1,
    Net           = 1u << 2,
    Proc          = 1u << 3,
    Gpu           = 1u << 4,
    ForceFullEmit = 1u << 5,
};
```

Extern declarations to REMOVE from src/btop_shared.hpp:
```cpp
// Line 493 (Gpu namespace):    extern vector<bool> redraw;
// Line 580 (Cpu namespace):    extern bool shown, redraw, got_sensors, cpu_temp_only, has_battery, supports_watts;
// Line 625 (Mem namespace):    extern bool has_swap, shown, redraw;
// Line 668 (Net namespace):    extern bool shown, redraw;
// Line 720 (Proc namespace):   extern bool shown, redraw;
```

CRITICAL: These are multi-variable extern declarations. Remove ONLY the `redraw` part, keeping other variables.
For example, `extern bool shown, redraw;` becomes `extern bool shown;`
And `extern bool has_swap, shown, redraw;` becomes `extern bool has_swap, shown;`
And for Cpu: `extern bool shown, redraw, got_sensors, ...` becomes `extern bool shown, got_sensors, ...`
For Gpu: delete the entire `extern vector<bool> redraw;` line.

Namespace-scope definitions in btop_draw.cpp that STAY (become file-local):
```cpp
// Line 1039 (Cpu):   bool shown = true, redraw = true, mid_line = false;  // KEEP
// Line 1629 (Gpu):   vector<bool> redraw = {};  // KEEP
// Line 1823 (Mem):   bool shown = true, redraw = true;  // KEEP
// Line 2090 (Net):   bool shown = true, redraw = true;  // KEEP
// Line 2205 (Proc):  bool shown = true, redraw = true;  // KEEP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove extern declarations and migrate all collector write sites</name>
  <files>src/btop_shared.hpp, src/linux/btop_collect.cpp, src/osx/btop_collect.cpp, src/freebsd/btop_collect.cpp, src/netbsd/btop_collect.cpp, src/openbsd/btop_collect.cpp</files>
  <action>
**Step 1: Remove extern declarations from btop_shared.hpp**

Edit the 5 extern declarations to remove only `redraw` from each:

- Line 493 (Gpu): Delete entire line `extern vector<bool> redraw;`
- Line 580 (Cpu): Change `extern bool shown, redraw, got_sensors, cpu_temp_only, has_battery, supports_watts;` to `extern bool shown, got_sensors, cpu_temp_only, has_battery, supports_watts;`
- Line 625 (Mem): Change `extern bool has_swap, shown, redraw;` to `extern bool has_swap, shown;`
- Line 668 (Net): Change `extern bool shown, redraw;` to `extern bool shown;`
- Line 720 (Proc): Change `extern bool shown, redraw;` to `extern bool shown;`

**Step 2: Migrate collector write sites across all 5 platforms**

For each platform's btop_collect.cpp, replace `redraw = true` with the appropriate `Runner::mark_dirty(DirtyBit::Xxx)` call based on which namespace function the site is in:

Determine namespace from surrounding function context:
- Sites inside `Mem::collect()` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Sites inside `Net::collect()` -> `Runner::mark_dirty(DirtyBit::Net);`
- Sites inside `Proc::collect()` -> `Runner::mark_dirty(DirtyBit::Proc);`

**Linux (src/linux/btop_collect.cpp):**
- Line 2315: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);` (Mem::collect, disk mount change)
- Line 2368: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);` (Mem::collect, disk count change)
- Line 2700: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);` (Net::collect, new interface)
- Line 2826: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);` (Net::collect, interface found)
- Line 2865: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);` (Net::collect, interface state)
- Line 2974: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);` (Proc::collect, detailed info)
- Line 3370: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);` (Proc::collect, dead status)

**macOS (src/osx/btop_collect.cpp):**
- Line 1292: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 1306: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 1397: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1533: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1572: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1661: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`
- Line 1873: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`

**FreeBSD (src/freebsd/btop_collect.cpp):**
- Line 710: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 724: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 802: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 939: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 978: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1066: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`
- Line 1242: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`

**NetBSD (src/netbsd/btop_collect.cpp):**
- Line 801: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 815: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 893: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1030: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1069: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1156: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`
- Line 1307: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`

**OpenBSD (src/openbsd/btop_collect.cpp):**
- Line 668: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 682: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Mem);`
- Line 760: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 897: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 936: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Net);`
- Line 1023: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`
- Line 1174: `redraw = true` -> `Runner::mark_dirty(DirtyBit::Proc);`

All collectors already `#include "../btop_shared.hpp"` which declares `Runner::mark_dirty()`. No new includes needed.

Do NOT touch any `redraw = true` sites in btop_draw.cpp (those are local variables, not externs).
Do NOT touch any `redraw` sites in btop_menu.cpp (Menu::redraw is out of scope).
  </action>
  <verify>
    <automated>grep -rn "extern.*redraw" src/btop_shared.hpp; echo "---"; grep -rn "redraw\s*=\s*true" src/*/btop_collect.cpp; echo "---"; echo "Both greps should return empty"</automated>
  </verify>
  <done>All 5 extern redraw declarations removed from btop_shared.hpp. All 35 collector redraw=true sites replaced with Runner::mark_dirty(DirtyBit::Xxx). No changes to btop_draw.cpp namespace-scope definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and full test suite</name>
  <files></files>
  <action>
Build the project and run the full test suite to verify the migration is correct.

1. Build: `cmake --build build-test -t btop_test 2>&1 | tail -20`
   - Must compile clean with no errors or warnings related to `redraw`
   - If build-test directory does not exist, configure first: `cmake -B build-test -DCMAKE_BUILD_TYPE=Debug -DENABLE_TESTING=ON && cmake --build build-test -t btop_test`

2. Run tests: `./build-test/tests/btop_test`
   - All 336+ tests must pass

3. Final grep validation:
   - `grep -rn "extern.*redraw" src/btop_shared.hpp` -- must return empty (MIGR-06)
   - `grep -rn "redraw\s*=\s*true" src/*/btop_collect.cpp` -- must return empty (MIGR-01 through MIGR-05)
   - `grep -rn "redraw\s*=\s*true" src/btop_draw.cpp` -- should show ONLY the local variable sites (lines ~1039, 1823, 2090, 2205) confirming they are preserved
   - `grep -rn "Runner::mark_dirty" src/*/btop_collect.cpp | wc -l` -- should show 35 (one per migrated site)
  </action>
  <verify>
    <automated>cmake --build build-test -t btop_test 2>&1 | tail -5 && ./build-test/tests/btop_test --gtest_brief=1 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with no redraw-related errors. All tests pass. Grep confirms zero extern redraw declarations and zero collector redraw=true sites. 35 Runner::mark_dirty calls present in collectors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "extern.*redraw" src/btop_shared.hpp` returns empty -- all extern declarations removed
2. `grep -rn "redraw\s*=\s*true" src/*/btop_collect.cpp` returns empty -- all collector sites migrated
3. `grep -rn "Runner::mark_dirty" src/*/btop_collect.cpp | wc -l` returns 35 -- all sites replaced
4. `grep -rn "redraw\s*=\s*true" src/btop_draw.cpp` returns only 4 local variable definitions -- self-invalidation preserved
5. Full test suite passes (336+ tests)
6. Build compiles clean
</verification>

<success_criteria>
- Zero extern redraw declarations in btop_shared.hpp
- Zero `redraw = true` assignments in any platform btop_collect.cpp
- 35 `Runner::mark_dirty(DirtyBit::Xxx)` calls across 5 platform collectors
- Local redraw variables in btop_draw.cpp preserved (Cpu, Gpu, Mem, Net, Proc)
- All tests pass with no regressions
- MIGR-01 through MIGR-06 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/34-per-box-bool-migration/34-01-SUMMARY.md`
</output>
