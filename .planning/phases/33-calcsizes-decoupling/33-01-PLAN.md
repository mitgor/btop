---
phase: 33-calcsizes-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/btop_draw.cpp]
autonomous: true
requirements: [DECPL-01, DECPL-02, DECPL-03]

must_haves:
  truths:
    - "calcSizes() contains zero direct assignments to per-namespace redraw bools"
    - "Resize, config-reload, box-toggle, preset-change, and disk-toggle all trigger full box redraw through dirty bits"
    - "request_redraw() uses pending_dirty.mark() (confirmed from Phase 32)"
  artifacts:
    - path: "src/btop_draw.cpp"
      provides: "calcSizes() with direct bool assignment removed"
      contains: "Runner::request_redraw()"
      not_contains: "Cpu::redraw = Mem::redraw"
  key_links:
    - from: "src/btop_draw.cpp calcSizes()"
      to: "Runner::request_redraw()"
      via: "function call on line 2923"
      pattern: "Runner::request_redraw"
    - from: "Runner::request_redraw()"
      to: "pending_dirty.mark()"
      via: "DirtyAll | ForceFullEmit"
      pattern: "pending_dirty\\.mark"
---

<objective>
Remove the redundant direct per-namespace redraw bool assignment from calcSizes(), completing the decoupling of geometry computation from redraw forcing. After Phase 32 wired PendingDirty into the runner, the direct `Cpu::redraw = Mem::redraw = Net::redraw = Proc::redraw = true` assignment on line 2936 of btop_draw.cpp is dead code -- request_redraw() on line 2923 already marks all dirty bits, and the runner decomposes them into per-box force_redraw=true for each draw function.

Purpose: Eliminate the legacy direct-bool redraw path so all redraw signaling flows exclusively through PendingDirty
Output: btop_draw.cpp with line 2936 removed, all tests passing
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-calcsizes-decoupling/33-RESEARCH.md
@.planning/phases/32-runner-integration/32-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 31-32. Executor needs these for understanding. -->

From src/btop_dirty.hpp:
```cpp
enum class DirtyBit : uint32_t { Cpu, Mem, Net, Proc, Gpu, ForceFullEmit, All };
constexpr uint32_t DirtyAll = /* all per-box bits */;
struct PendingDirty {
    void mark(DirtyBit bits) noexcept;  // atomic fetch_or
    void mark(uint32_t bits) noexcept;  // atomic fetch_or
    uint32_t take() noexcept;           // atomic exchange(0)
};
```

From src/btop.cpp:
```cpp
static PendingDirty pending_dirty;           // line 430
void request_redraw() noexcept {             // line 432
    pending_dirty.mark(DirtyAll | DirtyBit::ForceFullEmit);
}
void mark_dirty(DirtyBit bits) noexcept {    // line 436
    pending_dirty.mark(bits);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant bool assignment from calcSizes</name>
  <files>src/btop_draw.cpp</files>
  <action>
Remove line 2936 from calcSizes() in src/btop_draw.cpp:
```
Cpu::redraw = Mem::redraw = Net::redraw = Proc::redraw = true;
```

This line is redundant because line 2923 calls `Runner::request_redraw()` which marks `DirtyAll | ForceFullEmit` via `pending_dirty.mark()`. The runner thread's `take()` decomposes these into per-box `force_redraw=true` parameters passed to each draw function, which then set their own `redraw = true`.

IMPORTANT constraints:
- Do NOT touch line 2923 (`Runner::request_redraw()`) -- that is the new path and must stay
- Do NOT touch line 2928 (`Menu::redraw = true`) -- Menu::redraw is explicitly out of scope per REQUIREMENTS.md
- Remove ONLY the single assignment line (2936). Keep surrounding geometry assignments (x, y, width, height on lines 2932-2935) intact
  </action>
  <verify>
    <automated>grep -n "Cpu::redraw.*=.*Mem::redraw.*=.*Net::redraw.*=.*Proc::redraw" src/btop_draw.cpp | wc -l | tr -d ' ' | grep -q '^0$' && echo "PASS: no direct bool assignment" || echo "FAIL: direct bool assignment still present"</automated>
  </verify>
  <done>Line 2936 removed. calcSizes() contains zero direct assignments to per-namespace redraw bools. Runner::request_redraw() on line 2923 remains as sole dirty-marking mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and call-site validation</name>
  <files>src/btop_draw.cpp</files>
  <action>
Build the project and run the full test suite to verify no regressions from removing the redundant bool assignment.

Then verify all requirements:

DECPL-01: Confirm no direct per-namespace bool assignments remain in calcSizes():
  `grep -n "Cpu::redraw\|Mem::redraw\|Net::redraw\|Proc::redraw" src/btop_draw.cpp`
  Should return zero results (the only occurrence was line 2936 which was removed).

DECPL-02: Verify all 6 calcSizes() call sites still produce correct dirty state by confirming:
  1. btop.cpp Resizing entry action -- has mark_dirty(DirtyAll | ForceFullEmit) + run("all", true)
  2. btop.cpp Benchmark mode -- passes force_redraw=true directly to draw functions
  3. btop.cpp Main init -- first run() starts runner which calls take()
  4. btop_input.cpp box toggle -- has mark_dirty(DirtyAll | ForceFullEmit) + run("all", false)
  5. btop_input.cpp preset change -- has mark_dirty(DirtyAll | ForceFullEmit) + run("all", false)
  6. btop_input.cpp disk toggle -- has mark_dirty(DirtyBit::Mem) + run("mem", no_update)
  7. btop_menu.cpp screen_redraw -- has mark_dirty(DirtyAll | ForceFullEmit) + run("all", false)
  Each site either calls mark_dirty() after calcSizes(), or calcSizes() internally calls request_redraw() which marks dirty bits. Both paths are active and sufficient.

DECPL-03: Confirm request_redraw() uses PendingDirty (already done in Phase 32):
  `grep -A2 "request_redraw" src/btop.cpp` should show pending_dirty.mark(DirtyAll | DirtyBit::ForceFullEmit)
  </action>
  <verify>
    <automated>cmake --build build-test -t btop_test 2>&1 | tail -5 && ./build-test/tests/btop_test 2>&1 | tail -3</automated>
  </verify>
  <done>All 336+ tests pass. grep confirms zero per-namespace redraw bool assignments in btop_draw.cpp. grep confirms request_redraw() uses pending_dirty.mark(). All 6 calcSizes() call sites verified to produce correct dirty state through PendingDirty mechanism.</done>
</task>

</tasks>

<verification>
1. `grep -n "Cpu::redraw\|Mem::redraw\|Net::redraw\|Proc::redraw" src/btop_draw.cpp` returns zero results
2. `grep -n "Runner::request_redraw" src/btop_draw.cpp` returns the existing call (line ~2923)
3. `grep -n "Menu::redraw" src/btop_draw.cpp` still shows Menu::redraw = true (not removed)
4. `cmake --build build-test -t btop_test && ./build-test/tests/btop_test` all tests pass
5. `grep -A2 "request_redraw" src/btop.cpp` shows pending_dirty.mark usage
</verification>

<success_criteria>
- calcSizes() body contains zero direct assignments to Cpu::redraw, Mem::redraw, Net::redraw, or Proc::redraw
- Runner::request_redraw() call remains on line ~2923 as sole dirty-marking mechanism
- Menu::redraw = true assignment is preserved (out of scope)
- Full test suite (336+ tests) passes with no regressions
- request_redraw() confirmed to use pending_dirty.mark(DirtyAll | ForceFullEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/33-calcsizes-decoupling/33-01-SUMMARY.md`
</output>
