---
phase: 29-draw-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/btop_draw.cpp
autonomous: true
requirements:
  - DRAW-02
  - DRAW-03

must_haves:
  truths:
    - "Cpu::draw() top-level function is under 100 lines and delegates to named sub-functions"
    - "Battery state tracking is extracted into a separate draw_cpu_battery() function"
    - "Graph initialization and rendering are handled by separate sub-functions"
    - "Core text/graph rendering is handled by a separate sub-function"
    - "Terminal output is byte-identical before and after decomposition"
  artifacts:
    - path: "src/btop_draw.cpp"
      provides: "Decomposed Cpu::draw() with sub-functions"
      contains: "draw_cpu_battery"
  key_links:
    - from: "Cpu::draw()"
      to: "draw_cpu_battery(), draw_cpu_redraw(), draw_cpu_graphs(), draw_cpu_cores(), draw_cpu_footer()"
      via: "direct function calls from top-level draw()"
      pattern: "draw_cpu_(battery|redraw|graphs|cores|footer)"
---

<objective>
Decompose Cpu::draw() (454 lines) into focused sub-functions with battery state tracking extracted into a separate function, and no change in rendered output.

Purpose: Cpu::draw() is a god function handling graph initialization, battery display with static state, per-core text rendering, GPU brief info, load averages, and button rendering all in one function. Extracting battery state tracking and other logical blocks improves maintainability.

Output: Cpu::draw() under 100 lines calling 4-5 named helper functions, with identical terminal output.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/btop_draw.cpp (namespace Cpu, lines 1028-1506)
@src/btop_draw.hpp

<interfaces>
<!-- The Cpu namespace in btop_draw.cpp (lines 1028-1048) has these module-level variables
     that all sub-functions will need access to. -->

From src/btop_draw.cpp (Cpu namespace state, lines 1028-1048):
```cpp
namespace Cpu {
    int width_p = 100, height_p = 32;
    int min_width = 60, min_height = 8;
    int x = 1, y = 1, width = 20, height;
    int b_columns, b_column_size;
    int b_x, b_y, b_width, b_height;
    float max_observed_pwr = 1.0f;
    int graph_up_height, graph_low_height;
    int graph_up_width, graph_low_width;
    int gpu_meter_width;
    bool shown = true, redraw = true, mid_line = false;
    string box;
    vector<Draw::Graph> graphs_upper;
    vector<Draw::Graph> graphs_lower;
    Draw::Meter cpu_meter;
    vector<Draw::Meter> gpu_meters;
    vector<Draw::Graph> core_graphs;
    vector<Draw::Graph> temp_graphs;
    vector<Draw::Graph> gpu_temp_graphs;
    vector<Draw::Graph> gpu_mem_graphs;
}
```

From src/btop_draw.cpp (Cpu::draw signature, lines 1050-1057):
```cpp
string draw(
    const cpu_info& cpu,
#if defined(GPU_SUPPORT)
    const vector<Gpu::gpu_info>& gpus,
#endif
    bool force_redraw,
    bool data_same
);
```

The draw function returns a string containing escape sequences for terminal output.
Sub-functions should append to the same `out` string (passed by reference).

Key types from btop_shared.hpp used in Cpu::draw:
- cpu_info: struct with cpu_percent, core_percent, temp, load_avg, etc.
- gpu_info: struct with gpu_percent, temp, mem_used, mem_total, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Cpu::draw() sub-functions and reduce top-level to under 100 lines</name>
  <files>src/btop_draw.cpp</files>
  <action>
Decompose Cpu::draw() (lines 1050-1504) into named sub-functions within the Cpu namespace. All sub-functions are file-local (not declared in the header).

**IMPORTANT:** Plan 01 (Proc::draw decomposition) may execute in parallel, also modifying btop_draw.cpp. However, the two plans touch DIFFERENT namespaces (Proc vs Cpu) with no overlap in line ranges. If Plan 01 has already been applied, line numbers may have shifted for the Cpu namespace -- locate the namespace by searching for `namespace Cpu {` and the draw function by searching for `string draw(` within that namespace.

**Sub-functions to extract:**

1. **`draw_cpu_redraw()`** (the `if (redraw) { ... }` block, lines ~1101-1240) - Box outline, title buttons, graph/meter initialization, mid-line divider, core graphs, temp graphs.
   - Signature: `void draw_cpu_redraw(string& out, const cpu_info& cpu, GPU_PARAMS, bool single_graph, bool show_temps, bool hide_cores, bool show_watts, int extra_width, const string& graph_up_field, const string& graph_lo_field, const string& graph_symbol, const string& graph_bg, const string& temp_scale, bool cpu_bottom, const string& title_left, const string& title_right, bool tty_mode, SHOW_GPU_PARAMS)`
   - Note: GPU_PARAMS / SHOW_GPU_PARAMS are the `#ifdef GPU_SUPPORT` conditional parameters. Keep all `#ifdef` guards intact.
   - This function handles: box, buttons on title, container engine name, init_graphs lambda, core_graphs, temp_graphs initialization.

2. **`draw_cpu_battery()`** (lines ~1242-1283) - Battery state display with all its static local variables.
   - Signature: `void draw_cpu_battery(string& out, const string& title_left, const string& title_right, bool cpu_bottom)`
   - This is the primary extraction called out in the requirements (DRAW-02).
   - Move ALL static locals (`old_percent`, `old_seconds`, `old_watts`, `old_status`, `bat_meter`, `bat_symbols`) INTO this function. They are already static locals, so this is a clean cut.
   - Keep the `bat_pos` and `bat_len` static variables -- they need to persist across calls. Move them into this sub-function as well (they are currently static locals at Cpu::draw scope, line 1090). Note `bat_pos`/`bat_len` are referenced both in the battery section AND in the `else if (bat_pos > 0)` cleanup. Solution: make `draw_cpu_battery()` return or update a namespace-level `bat_pos`/`bat_len` pair, OR keep them as static inside draw_cpu_battery and pass a reference. Simplest approach: make `bat_pos` and `bat_len` namespace-scoped variables in Cpu instead of static locals.

3. **`draw_cpu_graphs()`** (lines ~1285-1375, the try block) - CPU/GPU graph rendering, uptime display, cpu clock, cpu meter, watts display.
   - Signature: `void draw_cpu_graphs(string& out, const cpu_info& cpu, GPU_PARAMS, bool single_graph, bool show_temps, bool show_watts, bool data_same, const string& graph_up_field, const string& graph_lo_field, const string& graph_symbol, const string& graph_bg, const string& temp_scale, bool tty_mode, SHOW_GPU_PARAMS)`
   - Keep the try/catch for "graphs, clock, meter" error handling. The try/catch should wrap the sub-function call in the top-level draw(), or be inside the sub-function.

4. **`draw_cpu_cores()`** (lines ~1377-1450) - Per-core text, per-core graphs, load average.
   - Signature: `void draw_cpu_cores(string& out, const cpu_info& cpu, bool show_temps, bool hide_cores, int extra_width, bool data_same, const string& graph_symbol, const string& graph_bg, const string& temp_scale, int n_gpus_to_show)`
   - Includes both the core iteration loop and the load average section.

5. **`draw_cpu_gpu_info()`** (lines ~1452-1500, inside `#ifdef GPU_SUPPORT`) - GPU brief info rows in the CPU box.
   - Signature: `void draw_cpu_gpu_info(string& out, GPU_PARAMS, bool show_temps, bool data_same, const string& graph_symbol, const string& graph_bg, const string& temp_scale, int& cy, SHOW_GPU_PARAMS)`
   - Entire body is `#ifdef GPU_SUPPORT` guarded.

**Implementation approach:**
- Define all sub-functions ABOVE the `draw()` function but within `namespace Cpu { }`.
- Sub-functions are NOT added to the header file -- they are internal implementation details.
- The top-level `draw()` becomes a ~60-90 line orchestrator: config reads, field resolution, early returns, call redraw, call battery, call graphs, call cores, call gpu_info, set `redraw = false`, return.
- Do NOT change any logic, string concatenation, or formatting. Pure mechanical extraction.
- Preserve ALL `#ifdef GPU_SUPPORT` and `#ifdef __linux__` guards exactly as they are.
- The `out.reserve()` stays in the top-level draw().
- The `redraw = false` and `return out + Fx::reset` stay in the top-level draw().

**Critical: `bat_pos` and `bat_len` handling:**
- Currently `static int bat_pos = 0, bat_len = 0;` at line 1090 inside draw().
- They are used in both the battery block AND the `else if (bat_pos > 0)` cleanup at line 1280.
- Solution: Move `bat_pos` and `bat_len` to Cpu namespace scope (alongside `max_observed_pwr` etc.). Initialize to 0. Remove the `static` keyword. Then both draw_cpu_battery() and the else branch in draw() can access them.

**Critical correctness rule:** The sub-functions must produce EXACTLY the same string output. Do not reorder operations, change string concatenation, or modify any escape sequences. The decomposition is purely structural.

**Parameters vs namespace state:** Variables like `width`, `height`, `x`, `y`, `b_x`, `b_y`, `b_width`, `b_height`, `b_columns`, `b_column_size`, `graph_up_height`, `graph_low_height`, `graph_up_width`, `graph_low_width`, `mid_line`, `box`, `graphs_upper`, `graphs_lower`, `cpu_meter`, `gpu_meters`, `core_graphs`, `temp_graphs`, `gpu_temp_graphs`, `gpu_mem_graphs`, `max_observed_pwr`, `redraw` are all Cpu namespace-level. Sub-functions access them directly. Only LOCAL variables computed inside draw() before the extracted block need to be parameters.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/btop && cmake --build build-test --target btop 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Cpu::draw() top-level function is under 100 lines
    - Five sub-functions exist: draw_cpu_redraw, draw_cpu_battery, draw_cpu_graphs, draw_cpu_cores, draw_cpu_gpu_info
    - Battery state tracking (static locals) is fully contained within draw_cpu_battery()
    - bat_pos and bat_len are namespace-scoped in Cpu (not static locals in draw)
    - All sub-functions are within namespace Cpu but not in the header
    - Build succeeds with zero warnings related to the changes
    - No logic changes -- pure structural extraction
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Cpu::draw() byte-identical output and cross-platform build</name>
  <files>src/btop_draw.cpp</files>
  <action>
Verify that the decomposed Cpu::draw() produces byte-identical terminal output and the project builds correctly.

**Verification approach:**
1. Build btop successfully: `cmake --build build-test --target btop 2>&1 | tail -20`
2. Run existing tests: `cd build-test && ctest --output-on-failure 2>&1 | tail -30`
3. Count lines of Cpu::draw() top-level function body (from opening `{` to closing `}`) and confirm under 100 lines.
4. Grep for the sub-function names to confirm they exist: `grep -n 'draw_cpu_\(battery\|redraw\|graphs\|cores\|gpu_info\)' src/btop_draw.cpp`
5. Verify no new functions were added to the header: `grep -c 'draw_cpu_' src/btop_draw.hpp` should return 0.
6. Verify battery static locals are inside draw_cpu_battery: `grep -A5 'draw_cpu_battery' src/btop_draw.cpp | grep 'static'`

**Line count verification:** Use grep/awk to count lines between the Cpu::draw() signature and its matching closing `}`.

**If tests fail:** Investigate whether the failure is related to this change or pre-existing.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/btop && grep -c 'draw_cpu_' src/btop_draw.cpp && grep -c 'draw_cpu_' src/btop_draw.hpp && cd build-test && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Build succeeds on the current platform
    - All existing tests pass
    - Cpu::draw() top-level body is under 100 lines (verified by line count)
    - draw_cpu_battery() contains the battery static locals (old_percent, old_seconds, old_watts, old_status, bat_meter, bat_symbols)
    - bat_pos and bat_len are Cpu namespace-scoped (not static locals in draw)
    - Sub-function declarations do NOT appear in the header
    - grep confirms 5+ occurrences of draw_cpu_ helper names in btop_draw.cpp
  </done>
</task>

</tasks>

<verification>
1. `cmake --build build-test --target btop` completes with zero errors
2. `ctest --output-on-failure` -- all existing tests pass
3. Cpu::draw() body is under 100 lines (count from function signature to closing brace)
4. Five named sub-functions exist within Cpu namespace in btop_draw.cpp
5. Battery state tracking is fully encapsulated in draw_cpu_battery()
6. No new declarations added to btop_draw.hpp
7. No behavioral changes -- pure structural refactor
</verification>

<success_criteria>
- Cpu::draw() decomposed with battery state tracking extracted into separate function
- Top-level Cpu::draw() is under 100 lines
- Terminal output is byte-identical (verified by successful build and all tests passing)
- btop builds on macOS (current platform)
</success_criteria>

<output>
After completion, create `.planning/phases/29-draw-decomposition/29-02-SUMMARY.md`
</output>
