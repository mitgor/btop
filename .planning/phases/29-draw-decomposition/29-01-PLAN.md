---
phase: 29-draw-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/btop_draw.cpp
autonomous: true
requirements:
  - DRAW-01
  - DRAW-03

must_haves:
  truths:
    - "Proc::draw() top-level function is under 100 lines and delegates to named sub-functions"
    - "Detailed view rendering is handled by a separate draw_proc_detailed() function"
    - "Process list rendering is handled by a separate draw_proc_list() function"
    - "Filter bar and title buttons are handled by a separate draw_proc_header() function"
    - "Terminal output is byte-identical before and after decomposition"
  artifacts:
    - path: "src/btop_draw.cpp"
      provides: "Decomposed Proc::draw() with sub-functions"
      contains: "draw_proc_detailed"
  key_links:
    - from: "Proc::draw()"
      to: "draw_proc_detailed(), draw_proc_header(), draw_proc_list()"
      via: "direct function calls from top-level draw()"
      pattern: "draw_proc_(detailed|header|list)"
---

<objective>
Decompose Proc::draw() (553 lines) into focused sub-functions with no change in rendered output.

Purpose: Proc::draw() is a god function that handles detailed view rendering, process list iteration, filter bar display, title buttons, scrollbar, and cleanup all in one function. Splitting into named sub-functions improves readability, testability, and maintainability.

Output: Proc::draw() under 100 lines calling 4-5 named helper functions, with identical terminal output.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/btop_draw.cpp (namespace Proc, lines 2087-2746)
@src/btop_draw.hpp

<interfaces>
<!-- The Proc namespace in btop_draw.cpp (lines 2087-2110) has these module-level variables
     that all sub-functions will need access to. They are namespace-scoped, not passed as parameters. -->

From src/btop_draw.cpp (Proc namespace state, lines 2087-2110):
```cpp
namespace Proc {
    int width_p = 55, height_p = 68;
    int min_width = 44, min_height = 16;
    int x, y, width = 20, height;
    int start, selected, select_max;
    bool shown = true, redraw = true;
    bool is_last_process_in_list = false;
    int selected_pid = 0, selected_depth = 0;
    int scroll_pos;
    string selected_name;
    std::unordered_map<size_t, Draw::Graph> p_graphs;
    std::unordered_map<size_t, bool> p_wide_cmd;
    std::unordered_map<size_t, int> p_counters;
    int counter = 0;
    Draw::TextEdit filter;
    Draw::Graph detailed_cpu_graph;
    Draw::Graph detailed_mem_graph;
    int user_size, thread_size, prog_size, cmd_size, tree_size;
    int dgraph_x, dgraph_width, d_width, d_x, d_y;
    bool previous_proc_banner_state = false;
    atomic<bool> resized (false);
    string box;
}
```

From src/btop_draw.cpp (Proc::draw signature, line 2191):
```cpp
string draw(const vector<proc_info>& plist, bool force_redraw, bool data_same);
```

The draw function returns a string containing escape sequences for terminal output.
All sub-functions should append to the same `out` string (passed by reference) or return string segments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Proc::draw() sub-functions and reduce top-level to under 100 lines</name>
  <files>src/btop_draw.cpp</files>
  <action>
Decompose Proc::draw() (lines 2191-2744) into named sub-functions within the Proc namespace. All sub-functions are file-local (not declared in the header) since they are implementation details.

**Strategy:** Each sub-function operates on the shared `out` string by reference and reads Proc namespace-level state. The sub-functions are anonymous-namespace or just namespace-scoped helpers above draw().

**Sub-functions to extract:**

1. **`draw_proc_follow_logic()`** (lines 2222-2278) - Handle follow-process state, restore-detailed-pid, banner state changes, selection edge cases.
   - Signature: `void draw_proc_follow_logic(const vector<proc_info>& plist, int& start, int& selected, int select_max, bool follow_process, int followed_pid, int& followed, bool should_selection_return_to_followed, bool pause_proc_list, bool proc_tree, bool proc_banner_shown, int numpids)`
   - This modifies `start`, `selected`, `followed` and various Config values.
   - Note: Keep the `previous_proc_banner_state` and `is_last_process_in_list` logic that triggers redraws near the top level OR inside this function since it sets `redraw`.

2. **`draw_proc_header()`** (lines 2280-2480, the `if (redraw)` block) - Draw box outline, title buttons, filter bar, sorting controls, field column labels, detailed view chrome.
   - Signature: `void draw_proc_header(string& out, int y, int height, int select_max, bool show_detailed, bool proc_tree, bool proc_banner_shown, bool follow_process, bool pause_proc_list, bool vim_keys, bool mem_bytes, bool show_graphs, int numpids, int followed_pid, bool should_selection_return_to_followed, bool data_same, const string& graph_symbol, const string& graph_bg, uint64_t totalMem)`
   - This is the entire `if (redraw) { ... }` block. It also sets mouse_mappings, creates graphs, etc.
   - Include the detailed box structure rendering (lines 2302-2381) within this function.

3. **`draw_proc_detailed()`** (lines 2484-2523) - Render the detailed process info section (CPU graph, status, memory, etc.) that appears every cycle when detailed view is shown.
   - Signature: `void draw_proc_detailed(string& out, bool show_detailed, bool data_same, uint64_t totalMem, bool pause_proc_list, const string& graph_bg)`
   - Uses `detailed`, `detailed_cpu_graph`, `detailed_mem_graph`, `d_width`, `d_x`, `d_y`, `dgraph_x` from namespace scope.

4. **`draw_proc_list()`** (lines 2535-2674) - The main per-process row rendering loop.
   - Signature: `void draw_proc_list(string& out, const vector<proc_info>& plist, int y, int height, int start, int selected, int select_max, bool proc_tree, bool proc_gradient, bool proc_colors, bool show_graphs, bool mem_bytes, bool data_same, bool follow_process, int followed_pid, uint64_t totalMem, const string& graph_symbol, const string& graph_bg)`
   - Returns the `lc` count (lines consumed) via an output parameter or return value so the caller can do post-list rendering.

5. **`draw_proc_footer()`** (lines 2676-2743) - Blank remaining lines, banner bar, scrollbar, location counter, dead-process graph cleanup, hide button.
   - Signature: `void draw_proc_footer(string& out, int y, int height, int lc, int start, int selected, bool proc_banner_shown, bool pause_proc_list, bool follow_process, int followed_pid, int followed, bool show_detailed, int numpids, int select_max, bool data_same)`

**Implementation approach:**
- Define all sub-functions ABOVE the `draw()` function but within `namespace Proc { }`.
- Sub-functions are NOT added to the header file -- they are internal implementation details.
- The top-level `draw()` becomes a ~60-80 line orchestrator: config reads, early returns, call follow_logic, call header (inside `if(redraw)`), call detailed, bounds check, call list, call footer, return.
- Do NOT change any logic, string concatenation, or formatting. This is a pure mechanical extraction (cut-and-paste into functions with parameters for any local variables that were computed in draw() before the extracted block).
- Preserve ALL `#ifdef GPU_SUPPORT` guards exactly as they are.
- The `out.reserve()` stays in the top-level draw().
- The `redraw = false` and `return out + Fx::reset` stay in the top-level draw().

**Critical correctness rule:** The sub-functions must produce EXACTLY the same string output. Do not reorder operations, change string concatenation, or modify any escape sequences. The decomposition is purely structural.

**Parameters vs namespace state:** Variables like `width`, `height`, `x`, `y`, `d_x`, `d_y`, `d_width`, `dgraph_x`, `dgraph_width`, `selected_pid`, `selected_name`, `selected_depth`, `p_graphs`, `p_counters`, `p_wide_cmd`, `counter`, `box`, `filter`, `detailed_cpu_graph`, `detailed_mem_graph`, `user_size`, `thread_size`, `prog_size`, `cmd_size`, `tree_size`, `scroll_pos`, `redraw`, `is_last_process_in_list` are all Proc namespace-level. Sub-functions access them directly (no parameter needed). Only LOCAL variables computed inside draw() before the extracted block need to be parameters.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/btop && cmake --build build-test --target btop 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Proc::draw() top-level function is under 100 lines
    - Five sub-functions exist: draw_proc_follow_logic, draw_proc_header, draw_proc_detailed, draw_proc_list, draw_proc_footer
    - All sub-functions are within namespace Proc but not in the header
    - Build succeeds with zero warnings related to the changes
    - No logic changes -- pure structural extraction
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Proc::draw() byte-identical output</name>
  <files>src/btop_draw.cpp</files>
  <action>
Verify that the decomposed Proc::draw() produces byte-identical terminal output.

**Verification approach:**
1. Build btop successfully: `cmake --build build-test --target btop 2>&1 | tail -20`
2. Run existing tests: `cd build-test && ctest --output-on-failure 2>&1 | tail -30`
3. Count lines of Proc::draw() top-level function body (from opening `{` to closing `}`) and confirm under 100 lines.
4. Grep for the sub-function names to confirm they exist: `grep -n 'draw_proc_\(detailed\|header\|list\|footer\|follow_logic\)' src/btop_draw.cpp`
5. Verify no new functions were added to the header: `grep -c 'draw_proc_' src/btop_draw.hpp` should return 0.

**If tests fail:** Investigate whether the failure is related to this change or pre-existing. The decomposition should be behavior-preserving.

**Line count verification:** Use grep/awk to count lines between `string draw(const vector<proc_info>` and the matching closing `}` in the Proc namespace.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/btop && grep -c 'draw_proc_' src/btop_draw.cpp && grep -c 'draw_proc_' src/btop_draw.hpp && cd build-test && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Build succeeds on the current platform
    - All existing tests pass
    - Proc::draw() top-level body is under 100 lines (verified by line count)
    - Sub-function declarations do NOT appear in the header
    - grep confirms 5+ occurrences of draw_proc_ helper names in btop_draw.cpp
  </done>
</task>

</tasks>

<verification>
1. `cmake --build build-test --target btop` completes with zero errors
2. `ctest --output-on-failure` -- all existing tests pass
3. Proc::draw() body is under 100 lines (count from function signature to closing brace)
4. Five named sub-functions exist within Proc namespace in btop_draw.cpp
5. No new declarations added to btop_draw.hpp
6. No behavioral changes -- pure structural refactor
</verification>

<success_criteria>
- Proc::draw() decomposed into named sub-functions (detailed view rendering, process list rendering, filter bar display, follow logic, footer)
- Top-level Proc::draw() is under 100 lines
- Terminal output is byte-identical (verified by successful build and all tests passing)
- btop builds on macOS (current platform)
</success_criteria>

<output>
After completion, create `.planning/phases/29-draw-decomposition/29-01-SUMMARY.md`
</output>
